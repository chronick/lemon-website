# LEMON AUDIO

Teaser site for LEMON AUDIO — レモン 🍋

## Development

Open `public/index.html` in a browser. No build step required.

## Deployment

Deployed to GitHub Pages via the `deploy` workflow. Pushes to `main` automatically publish the contents of `public/`.
